// function getWordLengths(inputString) {
//     // Split the string into words
//     const words = inputString.split(' '); // Using a regular expression to split by whitespace

//     // Map the array of words to an array of their lengths
//     const wordLengths = words.map(word => word.length);

//     return wordLengths;
// }

// // Example usage:
// const inputString = 'This is an example sentence.';
// const lengthsArray = getWordLengths(inputString);

// console.log('Word lengths array:', lengthsArray);

let num = 0

if (Math.random(num) > 10 && Math.random(num) < 20) {
    console.log(num);
}





